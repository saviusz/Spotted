import moment from "moment";
import replaceMarkers from "./utils/replaceMakers";

export const addListiners = () => {};

/* function addListinerFor(params: type) {} */

export { moment, replaceMarkers };
